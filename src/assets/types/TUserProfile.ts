export interface TUserProfile {
    id: number,
    nickname: string,
    description: string,
    avatarURL: string,
    clapCount: number,
    scrapCount: number,
    myTravelDay: number,
    myTravelCount: number,
}